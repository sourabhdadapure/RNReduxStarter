import React from 'react'
import styled from 'styled-components'

const StyledText = styled.Text`
  text-align: center;
  font-size: 20;
`

const Header = () => <StyledText>Sign Up</StyledText>

export default Header
